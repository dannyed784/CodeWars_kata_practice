string.split() --> return an array

array.join() --> return an string

array.reverse() (reverse the elements of the array) --> return an array

array.forEach(function callback(currentValue,index,array){
    //iterator
}[,thisArg]);

array.map() --> return a newArray 
Sintaxis:
var newArray = arr.map(function callback(currentValue,index,array){
    //elemento devuelto de nuevo_array
}[,thisArg]);


METODO Math
Math.min(numbers) --> return minor number (recommended to spread the numbers if it is an array)
Math.max(numbers) --> return mayor number (recommended to spread the numbers if it is an array)

