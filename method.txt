***Method Math***
Math.min(numbers) --> return minor number (recommended to spread the numbers if it is an array)
Math.max(numbers) --> return mayor number (recommended to spread the numbers if it is an array)

***Object.entries()***
El método Object.entries() devuelve una matriz(array) de pares propios de una propiedad enumerable [key, value] de un objeto dado, en el mismo orden que es proporcionado por for...in (La diferencia es que un bucle for-in enumera las propiedades en la cadena de prototipos).

sintaxis:
Object.entries(obj);

Obj:
The object whose enumerable own property [key, value] pairs are to be returned.
Array:
An array of the given object's own enumerable property [key, value] pairs.

**Method toFixed**
return de number with the numberOFdecimal 
Number.toFixed(numerOfdecimals)
sintaxis:
numObj.toFixed(numero de decimales)


